export default function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>CoreCraft — Atividade 2</h1>
      <p>Frontend Vite (esqueleto). API em dev: prefixo <code>/api</code> → backend.</p>
    </main>
  );
}
